CREATE TABLE "BOOK" (
  "ID" BIGINT GENERATED BY DEFAULT AS IDENTITY,
  "AUTHOR" VARCHAR(255),
  "BOOKNAME" VARCHAR(255),
  "GENRE" VARCHAR(255),
  "PUBLISH_DATE" DATETIME ,
  PRIMARY KEY ("ID")
);